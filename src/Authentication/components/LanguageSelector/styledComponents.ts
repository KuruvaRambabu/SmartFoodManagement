import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const LanguageSelectContainer = styled.select`
   ${tw`bg-white border rounded  focus:outline-none border-black`}
`

export const Languages = styled.option``
