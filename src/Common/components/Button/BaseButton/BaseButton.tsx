import React, { Component } from 'react'

import { BaseBtn } from './styledComponents'

class BaseButton extends Component {
   render() {
      return <BaseBtn apiStatus={100}></BaseBtn>
   }
}

export default BaseButton
