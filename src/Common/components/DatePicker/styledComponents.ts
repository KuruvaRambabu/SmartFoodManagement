import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const DatePickerMainContainer = styled.div`
   ${tw`w-full flex justify-center items-center `}
`
export const DateLabel = styled.span`
   ${tw` text-xl  mr-2 `}
`
