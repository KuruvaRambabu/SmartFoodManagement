import styled from '@emotion/styled'

import tw from 'tailwind.macro'

export const CustomDetailsMainContainer = styled.div`
   height: 350px;
   ${tw``}
`
export const RenderMealItemsMainContainer = styled.div`
   ${tw`h-20 flex justify-around`}
`

export const SelectCustomPreferencesContainer = styled.div`
   ${tw``}
`
