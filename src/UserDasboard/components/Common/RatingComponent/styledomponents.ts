import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const RatingComponentMainContainer = styled.div`
   ${tw`border `}
`
export const ItemDetailsComponent = styled.div`
   ${tw``}
`

export const QuantityRatingContainer = styled.div`
   ${tw``}
`
